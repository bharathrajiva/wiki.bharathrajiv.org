---
title: Logarithms
description: CAT Quant formulas, shortcuts, and traps for Logarithms.
slug: /cat/quants/algebra/logarithms
tags: [CAT, Quant, Algebra]
---

## Logarithms

Definition:

```text
log_a b = x means a^x = b
a > 0, a != 1, b > 0
```

Rules:

```text
log_a(xy) = log_a x + log_a y
log_a(x/y) = log_a x - log_a y
log_a(x^k) = k log_a x
log_a b = 1 / log_b a
log_a b = log_c b / log_c a
```

Special values:

```text
log_a 1 = 0
log_a a = 1
a^(log_a x) = x
```

Speed tricks:

- Convert logs to exponentials if stuck.
- Same base logs can be compared directly if base > 1.
- If 0 < base < 1, comparison direction reverses.

