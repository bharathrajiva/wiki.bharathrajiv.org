---
title: Coordinate Geometry
description: CAT Quant formulas, shortcuts, and traps for Coordinate Geometry.
slug: /cat/quants/geometry-mensuration/coordinate-geometry
tags: [CAT, Quant, Geometry and Mensuration]
---

## Coordinate Geometry

Distance:

```text
Distance between (x1,y1), (x2,y2)
= sqrt[(x2-x1)^2 + (y2-y1)^2]
```

Midpoint:

```text
((x1+x2)/2, (y1+y2)/2)
```

Section formula:

```text
Point dividing A(x1,y1), B(x2,y2) in ratio m:n internally:
((mx2 + nx1)/(m+n), (my2 + ny1)/(m+n))
```

Slope:

```text
m = (y2-y1)/(x2-x1)
Parallel lines: slopes equal
Perpendicular lines: m1*m2 = -1
```

Line equation:

```text
y = mx + c
y - y1 = m(x - x1)
```

Area of triangle:

```text
Area = 1/2 * |x1(y2-y3) + x2(y3-y1) + x3(y1-y2)|
```

Circle:

```text
(x-a)^2 + (y-b)^2 = r^2
Center = (a,b), radius = r
```

