---
import Default from '@astrojs/starlight/components/Footer.astro';

const isHomepage = Astro.locals.starlightRoute.id === '';
---

{
  isHomepage ? (
    <footer class="nexus-footer">Bharath Rajiv A - Engineering Nexus - Systems, AI, infrastructure, and edge hardware</footer>
  ) : (
    <Default>
      <slot />
    </Default>
  )
}
